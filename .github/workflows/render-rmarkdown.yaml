# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    paths: ['**.Rmd']

name: render-rmarkdown

jobs:
  render-rmarkdown:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: prereqs
        run: |
          sudo apt-get -y update && sudo apt-get install -y git-core libcurl4-openssl-dev

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2

      - uses: r-lib/actions/setup-renv@v2
      
      - uses: r-lib/actions/setup-tinytex@v2
      - run: tlmgr --version

      - name: Render CV
        run: Rscript -e 'rmarkdown::render("long_form_cv/long_form_cv_vitae.Rmd")'
      - name: Render CV
        run: Rscript -e 'rmarkdown::render("one_page_resume/one_page_resume_pagedown.Rmd")'
      - name: Update github.io site and commit results
        run: |
          cp one_page_resume/one_page_resume_pagedown . && mv one_page_resume_pagedown.html index.html
          git add long_form_cv/long_form_cv_vitae*
          git add one_page_resume/one_page_resume_pagedown*
          git add index.html
          git commit -m 'Re-built my-document' || echo "No changes to commit"
          git push origin || echo "No changes to commit"
      
          
      ### - name: Render Rmarkdown files and Commit Results
      #   run: |
      #     RMD_PATH=($(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep '[.]Rmd$'))
      #     Rscript -e 'for (f in commandArgs(TRUE)) if (file.exists(f)) rmarkdown::render(f, output_format="all")' ${RMD_PATH[*]}
      #     git config --local user.name "$GITHUB_ACTOR"
      #     git config --local user.email "$GITHUB_ACTOR@users.noreply.github.com"
      #     git commit ${RMD_PATH[*]/.Rmd/.md/.pdf/html} -m 'Re-build Rmarkdown files' || echo "No changes to commit"
      #     git push origin || echo "No changes to commit"
      #     cp one_page_resume/*.html ./index.html
